# Contributing

1. Create a feature branch.
2. Add a failing test for behavior changes.
3. Run `dotnet test InboxDock.sln`.
4. Keep Vault writes local, atomic, and non-overwriting.
5. Open a pull request with screenshots for UI changes.
